# Health API starter

FastAPI skeleton for the health-check task.

Install:

```
pip install -r requirements.txt
```

Run:

```
uvicorn app.main:app --reload
```

Implement `GET /health` so it returns `{"status":"ok"}`.
